-- 24.2. Да се дефинира функция, която по дадено естествено число n връща списък с цифрите му, четени от дясно наляво.

getDigits n
    | n < 10 = [n]
    | otherwise = n `mod` 10 : getDigits (n `div` 10)